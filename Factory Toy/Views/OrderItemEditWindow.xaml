<Window x:Class="Factory_Toy.Views.OrderItemEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование позиции заказа" Height="450" Width="400"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen">
    <Border Background="{StaticResource CardColor}"
        CornerRadius="10"
        Padding="15"
        Margin="10"
        Effect="{StaticResource DropShadow}">

        <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ID заказа:"/>
        <TextBox x:Name="OrderBox" Grid.Row="1"/>

        <TextBlock Text="ID товара:" Grid.Row="2"/>
        <TextBox x:Name="ProductBox" Grid.Row="3"/>

        <TextBlock Text="Количество:" Grid.Row="4"/>
        <TextBox x:Name="QuantityBox" Grid.Row="5"/>

        <CheckBox x:Name="CustomCheck" Content="Индивидуальный заказ" Grid.Row="6"/>

        <TextBlock Text="Описание:" Grid.Row="7"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="8"/>

        <TextBlock Text="Эскиз (путь к файлу):" Grid.Row="9"/>
        <TextBox x:Name="SketchBox" Grid.Row="10"/>

        <Button Content="Сохранить" Grid.Row="11" Height="30" Margin="0 10 0 0"
                Click="Save_Click"/>
    </Grid>
    </Border>

</Window>
