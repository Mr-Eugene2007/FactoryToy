<Window x:Class="Factory_Toy.Views.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование пользователя" Height="400" Width="400"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen">

    <Border Background="{StaticResource CardColor}"
        CornerRadius="10"
        Padding="15"
        Margin="10"
        Effect="{StaticResource DropShadow}">


        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Логин:"/>
            <TextBox x:Name="LoginBox" Grid.Row="1"/>

            <TextBlock Text="Пароль:" Grid.Row="2"/>
            <TextBox x:Name="PasswordBox" Grid.Row="3"/>

            <TextBlock Text="ФИО:" Grid.Row="4"/>
            <TextBox x:Name="FullNameBox" Grid.Row="5"/>

            <TextBlock Text="Email:" Grid.Row="6"/>
            <TextBox x:Name="EmailBox" Grid.Row="7"/>

            <TextBlock Text="Телефон:" Grid.Row="8"/>
            <TextBox x:Name="PhoneBox" Grid.Row="9"/>

            <TextBlock Text="ID роли:" Grid.Row="10"/>
            <TextBox x:Name="RoleBox" Grid.Row="11"/>

            <Button Content="Сохранить" Grid.Row="12" Height="30" Margin="0 10 0 0"
                Click="Save_Click"/>
        </Grid>
    </Border>
</Window>